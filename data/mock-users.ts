/**
 * MOCK DATA — Used for development/demo only.
 * Production user data comes from Supabase auth + team service.
 * TODO: Replace with real user/team member service when auth is wired.
 */
export const USERS: { id: string; name: string; initials: string; color: string }[] = [];
